# Mobile pubspec additions for Firebase Auth

Update `mobile/flutter/pubspec.yaml` to include these dependencies:

dependencies:
  firebase_core: ^2.8.0
  firebase_auth: ^4.4.0
  google_sign_in: ^6.1.0
  flutter_secure_storage: ^8.0.0
  http: ^0.13.6

Note: run `flutter pub get` after updating the file. In this commit the pubspec.yaml in the branch was updated to include Firebase in the next step; if you need me to modify the pubspec in-place I can push that change as well.
